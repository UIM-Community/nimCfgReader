{
  "name": "nimcfgreader",
  "version": "1.0.1",
  "description": "Parse Nimsoft (UIM) configuration file",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "lodash.set": "^4.3.2"
  },
  "devDependencies": {
    "benchmark": "^2.1.4"
  }
}
